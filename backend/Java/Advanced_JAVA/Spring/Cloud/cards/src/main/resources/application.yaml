server:
  port: 9000

spring:
  application:
    name: cards

  datasource:
    url: jdbc:mysql://localhost:3306/cardsdb
    username: root
    password: tiger

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/